// NOTE: This file is NOT a VALID .nm code.

// Leading comment

/* Multi-
    Line-
        Block-
            /*Comment *with* * stars! **
            */

func(arg1, arg2)	// Trailing comment
{
	var local_var0;
	var local_var1;
	var chain0, /* Inline Block Comment */chain1, chain2;

	// Some simple and complex numerical formulae:
	a = 1 + 2 + 3 + 4 - 5 * 6 / 3;
	b = -a + --c;
	d = a && c || !b;

	nameTest;
	assign0 = assign1;
	func(10, 0.4, null, false, true, "string", 'c', (--compound_var), (func2(a, b, c)));
	add0 = (add1 + add2);
	if (condition == 0)
	{
		action0();
	}
	elseif (condition >= 4) {
		action2();
	} else {
		return "value";
	};

	while (true)
	{
		++local_var0;
	};

	return true;
}